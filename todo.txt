• work on bitstream.rs, replicate the function of first_qr_code()
• data entry 🥳 tables!! 🎊 ... specifically:
    - error correction characteristics (pg. 41)
• penalty routine still mysterious - do more troubleshooting
• update penalty functions to not use get_row()
• mode overhead calculation for when to switch modes

going through the standards doc:
• data modes, mode switching (pg. 22)
    mostly done!! great job me!! kanji and ECI remain
• codeword blocks (pg. 52, refer pg. 54)
    table is done and very probably accurate. now i only need the shuffling code
• symbol encoding example (pg. 90)
• bitstream optimization  (pg. 92)

PITEOG stuff:
• deprecate the continuous-data "Img" type?
• update get_row() to handle sizes >128 (versions 28 and onward)
    idk if this is necessary